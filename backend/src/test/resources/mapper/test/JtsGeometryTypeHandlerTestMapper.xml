<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.surimap.maparea.geometry.validation.mapper.JtsGeometryTypeHandlerTestMapper">
    <resultMap id="GeometryProbeRowMap"
               type="com.surimap.maparea.geometry.validation.mapper.JtsGeometryTypeHandlerTestMapper$GeometryProbeRow">
        <id property="id" column="id"/>
        <result property="geometry" column="geometry" typeHandler="com.surimap.config.mybatis.JtsGeometryTypeHandler"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO search_area (
            id, operational_period_id, parent_search_area_id, name,
            area_level, geometry, status, version, created_by_account_id,
            created_at, updated_at
        )
        VALUES (
            #{id},
            #{opId},
            NULL,
            'type-handler-probe',
            'UNIT',
            ST_GeomFromEWKT(#{geometry, typeHandler=com.surimap.config.mybatis.JtsGeometryTypeHandler}),
            'ACTIVE',
            1,
            #{accountId},
            TIMESTAMPTZ '2026-04-28 01:00:00+00',
            TIMESTAMPTZ '2026-04-28 01:00:00+00'
        )
    </insert>

    <select id="findById" resultMap="GeometryProbeRowMap">
        SELECT id, ST_AsEWKT(geometry) AS geometry
        FROM search_area
        WHERE id = #{id}
    </select>
</mapper>
