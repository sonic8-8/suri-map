<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.surimap.incident.repository.IncidentMapper">
    <resultMap id="incidentRecordMap" type="com.surimap.incident.domain.IncidentRecord">
        <id property="id" column="id" typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="sourceIncidentId" column="source_incident_id" typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="openedAt" column="opened_at"/>
        <result property="closedAt" column="closed_at"/>
        <result property="closedByAccountId" column="closed_by_account_id"
                typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="version" column="version"/>
    </resultMap>

    <resultMap id="idempotencyRecordMap" type="com.surimap.incident.domain.IncidentImportIdempotencyRecord">
        <id property="id" column="id" typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="requestBodyHash" column="request_body_hash"/>
        <result property="requestPath" column="request_path"/>
        <result property="requestMethod" column="request_method"/>
        <result property="idempotencyStatus" column="idempotency_status"/>
        <result property="resultEntityId" column="result_entity_id"
                typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="resultEntityStatus" column="result_entity_status"/>
        <result property="resultEntityVersion" column="result_entity_version"/>
    </resultMap>

    <resultMap id="missingPersonRecordMap" type="com.surimap.incident.domain.MissingPersonRecord">
        <id property="incidentId" column="incident_id" typeHandler="com.surimap.config.mybatis.UuidTypeHandler"/>
        <result property="displayName" column="display_name"/>
        <result property="photoObjectKey" column="photo_object_key"/>
        <result property="appearanceText" column="appearance_text"/>
        <result property="lastSeenLocationText" column="last_seen_location_text"/>
        <result property="lastSeenAt" column="last_seen_at"/>
        <result property="importedAt" column="imported_at"/>
    </resultMap>

    <select id="findBySourceIncidentId" resultMap="incidentRecordMap">
        SELECT
            id,
            source_incident_id,
            title,
            status,
            opened_at,
            closed_at,
            closed_by_account_id,
            version
        FROM "incident"
        WHERE source_incident_id = #{sourceIncidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
    </select>

    <select id="findByIncidentId" resultMap="incidentRecordMap">
        SELECT
            id,
            source_incident_id,
            title,
            status,
            opened_at,
            closed_at,
            closed_by_account_id,
            version
        FROM "incident"
        WHERE id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
    </select>

    <select id="findImportIdempotencyRecord" resultMap="idempotencyRecordMap">
        SELECT
            id,
            idempotency_key,
            request_body_hash,
            request_path,
            request_method,
            idempotency_status,
            result_entity_id,
            result_entity_status,
            result_entity_version
        FROM idempotency_record
        WHERE idempotency_key = #{idempotencyKey}
          AND request_path = #{requestPath}
          AND request_method = #{requestMethod}
    </select>

    <insert id="insertImportIdempotencyRecord">
        INSERT INTO idempotency_record (
            id,
            idempotency_key,
            request_body_hash,
            request_path,
            request_method,
            idempotency_status,
            created_at,
            updated_at
        )
        VALUES (
            #{id, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{idempotencyKey},
            #{requestBodyHash},
            #{requestPath},
            #{requestMethod},
            #{idempotencyStatus},
            #{now},
            #{now}
        )
    </insert>

    <update id="completeImportIdempotencyRecord">
        UPDATE idempotency_record
        SET
            idempotency_status = 'COMPLETED',
            incident_id = #{resultEntityId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            response_status_code = #{responseStatusCode},
            response_body_json = #{responseBodyJson},
            response_body_format_version = 1,
            result_entity_type = 'incident',
            result_entity_id = #{resultEntityId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            result_entity_status = #{resultEntityStatus},
            result_entity_version = #{resultEntityVersion},
            updated_at = #{now}
        WHERE idempotency_key = #{idempotencyKey}
          AND request_path = #{requestPath}
          AND request_method = #{requestMethod}
    </update>

    <insert id="insertIncident">
        INSERT INTO "incident" (
            id,
            source_incident_id,
            title,
            status,
            opened_at,
            closed_at,
            closed_by_account_id,
            version,
            created_at,
            updated_at
        )
        VALUES (
            #{id, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{sourceIncidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{title},
            #{status},
            #{openedAt},
            NULL,
            NULL,
            #{version},
            #{now},
            #{now}
        )
    </insert>

    <insert id="insertMissingPerson">
        INSERT INTO missing_person (
            incident_id,
            display_name,
            photo_object_key,
            appearance_text,
            last_seen_location_text,
            last_seen_at,
            imported_at
        )
        VALUES (
            #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{displayName},
            #{photoObjectKey},
            #{appearanceText},
            #{lastSeenLocationText},
            #{lastSeenAt},
            #{importedAt}
        )
    </insert>

    <insert id="insertIncidentAssignment">
        INSERT INTO incident_assignment (
            id,
            incident_id,
            account_id,
            incident_role,
            assigned_at,
            revoked_at,
            created_at,
            updated_at
        )
        VALUES (
            #{id, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            (SELECT id FROM account WHERE login_id = #{accountId} OR id::text = #{accountId} LIMIT 1),
            #{incidentRole},
            #{assignedAt},
            NULL,
            #{now},
            #{now}
        )
    </insert>

    <insert id="insertIncidentAssignmentIfAbsent">
        INSERT INTO incident_assignment (
            id,
            incident_id,
            account_id,
            incident_role,
            assigned_at,
            revoked_at,
            created_at,
            updated_at
        )
        SELECT
            #{id, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            (SELECT id FROM account WHERE login_id = #{accountId} OR id::text = #{accountId} LIMIT 1),
            #{incidentRole},
            #{assignedAt},
            NULL,
            #{now},
            #{now}
        WHERE NOT EXISTS (
            SELECT 1
            FROM incident_assignment
            WHERE incident_id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
              AND account_id = (SELECT id FROM account WHERE login_id = #{accountId} OR id::text = #{accountId} LIMIT 1)
              AND revoked_at IS NULL
        )
    </insert>

    <update id="incrementIncidentVersion">
        UPDATE "incident"
        SET
            version = version + 1,
            updated_at = #{now}
        WHERE id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
    </update>

    <update id="closeIncident">
        <!-- 종료 상태 전이와 쓰기 차단 기준 버전 증가는 같은 사건 행 갱신에서 끝낸다. -->
        UPDATE "incident"
        SET
            status = 'CLOSED',
            closed_at = #{closedAt},
            <!-- PostgreSQL UUID 컬럼은 UuidTypeHandler가 OTHER로 받아야 문자열 변환 없이 바인딩된다. -->
            closed_by_account_id = #{closedByAccountId, jdbcType=OTHER, typeHandler=com.surimap.config.mybatis.UuidTypeHandler},
            version = version + 1,
            updated_at = #{closedAt}
        WHERE id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
          AND status = 'OPEN'
    </update>

    <delete id="deleteMissingPersonByIncidentId">
        <!-- S1-1 소유 활성 missing_person 행을 삭제한다. S1-3 패키지/로컬 파기는 INCIDENT_CLOSED 소비자가 처리한다. -->
        DELETE FROM missing_person
        WHERE incident_id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
    </delete>

    <select id="findActiveAssignmentAccountIds" resultType="string">
        SELECT account_id
        FROM incident_assignment
        WHERE incident_id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
          AND revoked_at IS NULL
        ORDER BY assigned_at ASC, account_id ASC
    </select>

    <select id="findMissingPersonByIncidentId" resultMap="missingPersonRecordMap">
        SELECT
            incident_id,
            display_name,
            photo_object_key,
            appearance_text,
            last_seen_location_text,
            last_seen_at,
            imported_at
        FROM missing_person
        WHERE incident_id = #{incidentId, typeHandler=com.surimap.config.mybatis.UuidTypeHandler}
    </select>
</mapper>
