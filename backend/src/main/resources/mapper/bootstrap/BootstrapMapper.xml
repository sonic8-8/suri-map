<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.surimap.domain.bootstrap.BootstrapMapper">
  <select id="migrationProbeCount" resultType="int">
    SELECT COUNT(*)
    FROM bootstrap_migration_probe
  </select>
</mapper>
